# Runnable Aftermath packages

Intentionally empty. Packages enter this tree only after their classification is
`supported` and the Aftermath runtime, field, market, sizing, and execution
contracts pass. CI applies strict forbidden-pattern checks to this tree.
